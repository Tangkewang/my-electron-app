{
  "name": "my-electron-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package-win": "electron-packager . MyElectronApp --platform=win32 --arch=x64 --out=dist --overwrite",
    "dist": "electron-builder -w"
  },
  "devDependencies": {
    "electron": "^13.0.1",
    "electron-packager": "^15.3.0",
    "electron-builder": "^22.11.7"
  }
}